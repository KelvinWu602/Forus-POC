# Forus-POC
Using Python script to run experiment on the algorithm design

# Usage
Please open a new branch source from the main branch. Create your own experiment folder, name it in format "experiment(yyyy-mm-dd)". Then commit to your own branch. 

Then, make a pull request once you are finished.
